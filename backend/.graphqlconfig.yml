projects:
  app:
    schemaPath: "src/schema.graphql"
    extensions:
      endpoints:
        default: ${env:PRISMA_ENDPOINT}
  prisma:
    schemaPath: "src/generated/prisma.graphql"
    extensions:
      prisma: prisma.yml