# import * from './generated/prisma.graphql'

type User {
  id: ID!
  name: String!
  email: String!
}

type Mutation {
  createUser(name: String! email: String!): User
}

type Query {
  users: [User]!
}